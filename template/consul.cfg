template {
	source = "/consul-template/nginx.tmpl.in"
	destination = "/consul-template/nginx.tmpl"
	command = "pkill -HUP consul-template"
}

template {
	source = "/consul-template/nginx.tmpl"
	destination = "/etc/nginx/nginx.conf"
	command = "/nginx-run.sh"
}
